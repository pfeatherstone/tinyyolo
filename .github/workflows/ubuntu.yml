name: Ubuntu

on:
  push:
    branches:
      - "**"   # all branches
  pull_request:
    branches:
      - "**"   # all branches

jobs:
  test:
    name: Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04]

    env:
      TEST_RESULTS_DIR: ${{ github.workspace }}/test-results

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest numpy torch torchvision einops onnxruntime onnscript 

      - name: Run pytest
        run: |
          mkdir -p "$TEST_RESULTS_DIR"
          cd src
          pytest --junitxml="$TEST_RESULTS_DIR/results.xml" test_unit.py

      - name: Upload test results (JUnit XML)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytorch-unit-test-results-${{ matrix.os }}
          path: $TEST_RESULTS_DIR/results.xml

      - name: Publish test report to PR
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: PyTorch Unit Tests (${{ matrix.os }})
          path: $TEST_RESULTS_DIR/results.xml
          reporter: java-junit
          fail-on-error: "false"